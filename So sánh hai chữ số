#include <stdio.h>
#include <math.h>

int main() {
int a, b;
	printf("Nhap vao gia tri so nguyen a, b: ");
	scanf_s("%d%d", &a, &b);
	if (a == b) {
		printf("%d = %d\n", a , b);
	}
	else if (a < b) {
		printf("%d < %d\n", a, b);
	}
	else
	{
		printf("%d > %d\n", a, b);
	}
	return 0;
}
