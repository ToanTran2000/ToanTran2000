#include <stdio.h>
#include <math.h>


int main() {
	unsigned int n;
	printf("Nhap so tu nhien n:");
	scanf_s("%d", &n);
	unsigned int sum = 0;
	unsigned int i = 1;
	for (; i <= n; i++) {
		sum += i;
	}
	printf("SUM = %d", sum);
	return 0;
}
